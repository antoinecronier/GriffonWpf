<UserControl x:Class="GriffonWpf.UserControls.UserCreateUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GriffonWpf.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer>
        <StackPanel>
            <Label x:Name="lblError" Visibility="Collapsed"/>
            <Label x:Name="lblFirstname"/>
            <TextBox x:Name="txtBFirstname" Text="{Binding Firstname}"/>
            <Label x:Name="lblLastname"/>
            <TextBox x:Name="txtBLastname" Text="{Binding Lastname}"/>
            <Label x:Name="lblDateOfBirth"/>
            <DatePickerTextBox x:Name="datepickerDateOfBirth" Text="{Binding DateOfBirth}"/>
            <Label x:Name="lblLogin"/>
            <TextBox x:Name="txtBLogin" Text="{Binding Login}"/>
            <Label x:Name="lblPassword"/>
            <TextBox x:Name="txtBPassword" Text="{Binding Password}"/>
            <Button x:Name="btnValidate">Validate</Button>
        </StackPanel>
    </ScrollViewer>
</UserControl>
